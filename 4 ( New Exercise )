#include <stdio.h>

void swap(int x, int y) {
    int temp = x;
    x = y;
    y = temp;
    printf("Inside function: a = %d, b = %d\n", x, y);
}

int main() {
    int a, b;
    printf("Enter two numbers: ");
    scanf("%d %d", &a, &b);
    swap(a, b);
    printf("After function call: a = %d, b = %d (unchanged)\n", a, b);
    return 0;
}


Output :

Enter two numbers: 10 20
Inside function: a = 20, b = 10
After function call: a = 10, b = 20 (unchanged)
